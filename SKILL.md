---
name: agent-acceptance
description: 对 AI Agent 或多 Agent 项目做全套工程验收并给档位结论（Demo / 原型 / 有限 Beta / 生产候选 / block）。当需要判断项目是否真需要 Agent、系统部件是否真实接线、评测考卷与判分是否可信、安全与权限底线是否达标、能否上线与交付、最终应给什么档时，用它产出第三方可复现的验收报告。
metadata:
  author: LPK
  version: "1.0.0"
  updated: "2026-09-03"
  spec: https://agentskills.io/specification
---

# agent-acceptance（Agent 验收检查）

> **本文件是整套技能的入口** = 说明 + 章级路由索引，不承载判据正文。
> 执行路径**只一跳**：看下方"章级路由索引"确定该查哪几章，点开直达 `references/` 对应章正文即可，中间不需要经过任何第二层索引。`00-global-map.md`（全貌图）与章正文**平级**放在 `references/`，只在想先扫一遍全貌、核对覆盖盲区、或扩容时读它，不是执行必经。

## 一句话定位

让开发者验证 Agent 从闭门造车变成开箱即用：不评模型强不强，只查"这个 Agent 项目拿不拿得出手、放不放得出去、该给什么档"。

## 本技能结构

> 本包采用 Agent Skills 官方规范（[specification](https://agentskills.io/specification)）的标准形态：技能 = 一个目录 = `SKILL.md`（frontmatter 元数据 + 指令入口）+ `skill.json`（与 frontmatter 双写同步的机器可读元数据）+ `references/`（按需加载的参考文档）。三层渐进式披露：元数据常驻（发现用），入口正文触发时加载（路由用），`references/` 各章按需读取（执行用）。引用保持一层深——只允许 `SKILL.md` 直达 `references/` 各正文，references 内同目录互链，不产生第三层。

**两级平铺，唯一入口直达正文**：

1. `SKILL.md`（本文件）= **唯一入口**。前半部分是说明（什么时候用、怎么走），后半部分"章级路由索引"是最细路由表——被检方拿得出什么就查哪层，**每行链接直达对应章正文**。
2. `references/` = **判据正文 + 全貌图（平级成员）**：
   - 章正文 `01` 到 `10` + [附录 A](references/附录A-元规则.md) = 判据落地（检查点、判据表、达标/不达标信号、输出物）。
   - [00-global-map](references/00-global-map.md) = 章树全貌（对象全景，穷尽该检查的全部面），**供全貌扫描与扩容，不是执行必经的第二层**。

没有第三层。层（P0-P3）与深度（L1/L2/L3）是标注在正文之上的"使用视角"，不另建文档、不做成菜单——正文一律按章落，路由表已按"被检方提供物"把层翻译成"该点开哪几章"。

## 什么时候用

- 要验收一个 Agent / 多 Agent 项目（自研或第三方交付），判它能不能立项、能不能上线、能给什么档。
- 要核对 Agent 项目是否真需要 Agent、系统部件是否真实接线、评测资产是否可信、安全底线是否达标、交付证据是否成立。
- 要产出一份第三方可复现的验收报告与档位结论。
- 不适用的场景（Out of Scope 细节见 [附录 A](references/附录A-元规则.md)）：模型基座与训练质量、纯 prompt 封装、通用公平性、组织级合规认证。

## 一次验收怎么走（先挑跑法，不用全跑）

**开跑前先对号入座——三种跑法，按你的情况选一条主线**：

| 你是哪种情况 | 主线（章） | 特点与产出 |
|---|---|---|
| 自研想快速初判（只有源码+文档，还没建评测/运维） | 1 -> 2 -> 7（至少红线 7.5）-> 10 | **一次会话能跑完**的 L1 短跑：出"体检初判"——哪些面达标、哪些层记"未评估"、初步档位。缺材料的层**不会拖垮结论，只记"未评估"并在 10 自动封顶**，报告写明缺的是哪层 |
| 上线前/第三方交付的完整验收 | 1 -> 2 -> 3-5 -> 6-9 -> 10 顺章推 | **分批跑**：每章一次会话，章收口出一张判定表并指明接哪章；10 汇总所有判定表出验收结论单。每章可开可合，不必一口气 |
| 甲方/合同/采购收货（审交付物与证据） | 1 + 9 + 10 为主，需要时补 3/7/8 | 以交付物与证据等级为主线做证据核验，深查按场景触发式追加，结论走 10 |

**三条预期，先讲清再开跑**：
- 技能是"体检单"不是"考试"——你没有的材料 = 该层记"未评估"，不是失败；结论会因此封顶并写明缺什么，帮你看到补建优先级。
- 只跑上面第一条主线（约等于 L1 必查主干）就能出可用初判；要冲"生产候选"档才需要上 L2/L3 深化。
- 完整跑法下，每一章单独作为一次任务交给执行者即可，章收口会写明"这次产出 + 下一步接哪章"。

选定跑法后照常执行：

1. 读 [01-范围、形态与档位](references/01-范围、形态与档位.md)：被检方口答或文档即够。先证是否真需要 Agent，再锁形态/技术栈/边界/成功标准/自主度档位，输出定档记录。
2. 按定档与形态决定后续哪些章上场；每章头部写明本层门槛（被检方拿得出什么才查得动）。
3. 逐章逐判点执行：达标/警示/不达标标记，写证据，不达标与警示落整改建议。
4. 走完全部可达层后进 [10-验收结论与档位](references/10-验收结论与档位.md) 收口：否决项先挡门、加权定档、未证实项不混入、给改进闭环。
5. 全流程遵守 [附录 A](references/附录A-元规则.md) 的检查者纪律（先看 A.1 不评什么、A.2 反模式自查）。
6. 判不了或超出判据范围的项：直接联网检索官方文档/教程，给出来源链接并标注检索时间；**不编造出处、不猜测数字、不依赖本地任何文件**。

## 章级路由索引（以"被检方拿得出的材料"为轴）

下表是本技能最细的路由：被检方能提供到什么程度，就能查到哪一层；拿不出即该层记"未评估"，不硬套。P = 提供物分层（使用轴）；章号与判点号是稳定主键。

| 能提供到什么 | 层 | 覆盖章 | 每章一句话 |
|---|---|---|---|
| 口答或文档（形态/技术栈/目标边界/成功标准/自主度） | P0 定档问卷 | [1 范围、形态与档位](references/01-范围、形态与档位.md) | 必经入口 = 选层问卷：证需要 Agent、锁形态栈边界成功标准自主档，出定档记录 |
| 以上再加源码 + 文档 | P1 组成可查 | [2 系统组成与真实接线](references/02-系统组成与真实接线.md) | 按组成拆开逐块查齐不齐、接没接线，只判实现真实可用 |
| 以上再加测试集 / 自评基建 / 判分口径 / 目标值 | P2 评测资产 | [3 考卷与评测集](references/03-考卷与评测集.md)、[4 判分规则](references/04-判分规则.md)、[5 指标与目标值](references/05-指标与目标值.md) | 3 尺子可不可信；4 怎么判才对；5 多少算好（P2 收官） |
| 以上再加可运行部署 + 观测/审计/上线/交付证据 | P3 运行与交付证据 | [6 运行可观测](references/06-运行可观测.md)、[7 安全与权限底线](references/07-安全与权限底线.md)、[8 生产就绪与上线](references/08-生产就绪与上线.md)、[9 交付物与证据链](references/09-交付物与证据链.md) | 6 过程可见可回放；7 安全底线必查不可跳；8 能不能放出去；9 交得出什么、哪条证据能进结论 |
| 以上各层结论齐 | 收口 | [10 验收结论与档位](references/10-验收结论与档位.md) | 合成定论：否决先行、加权定档、未证实项清单、改进回归 |
| 全程 | 检查者纪律 | [附录 A 元规则](references/附录A-元规则.md) | 不评什么 + 反模式自查，读完才够格当检查者 |

深度并存标注（另一视图，不另建文档）：L1 必查主干（约 P0 + P1 + 7 底线 + 6/8/9 基础件）；L2 深化（数字合格线 / judge 校准 / 反模式）；L3 专项触发（对抗深检 / 合规对照 / 9.6 业务六维）。P 轴管"这层需要给什么才能查"，L 轴管"这次查多深、哪些触发"。

## 打包与维护边界

- 打包单元 = 本技能文件夹整体（`agent-acceptance/` = `SKILL.md` 入口 + `skill.json` 元数据 + `references/` 判据正文）；本文件夹不依赖任何外部文件。
- 改判据只改 `references/` 对应章正文，不动章号与判点号（判点号是稳定主键，正文互引用它）；新增检查面先过 [00-global-map](references/00-global-map.md) 扩容三关再落正文。
- 技能作者的写作纪律、链接白名单、打包前自查清单等维护文档收在本目录 `docs/`（作者与维护者专用，执行验收无需读取；技能生态发布可不随包，但随项目仓库保留）。
