---
name: agent-acceptance
description: 对 AI Agent / 多 Agent（智能体、多智能体）项目做工程验收、评估与定档体检，档位为 Demo / 原型 / 有限 Beta / 生产候选 / block：不评模型强不强，只查它拿不拿得出手、放不放得出去、该给什么档，并产出可第三方复核的验收报告。需要判断项目是否真需要 Agent、系统组成与各部件是否真实接线、评测考卷与判分规则是否可信、安全与权限底线是否达标、能否上线与交付放行时，用它逐层检查，给出未证实项清单与改进闭环。
metadata:
  author: LPK
  version: "1.2.0"
  updated: "2026-09-04"
  spec: https://agentskills.io/specification
---

# agent-acceptance（Agent 验收检查）

> **本文件是整套技能的入口** = 说明 + 章级路由索引，不承载判据正文。
> 执行路径**只一跳**：看下方"章级路由索引"确定该查哪几章，点开直达 `references/` 对应章正文即可，中间不需要经过任何第二层索引。`00-全局地图.md`（全貌图）与章正文**平级**放在 `references/`，只在想先扫一遍全貌、核对覆盖盲区、或扩容时读它，不是执行必经。

## 一句话定位

让开发者验证 Agent 从闭门造车变成开箱即用：不评模型强不强，只查"这个 Agent 项目拿不拿得出手、放不放得出去、该给什么档"。

## 本技能结构

> 本包采用 Agent Skills 官方规范（[specification](https://agentskills.io/specification)）的标准形态：技能 = 一个目录 = `SKILL.md`（frontmatter 元数据 + 指令入口）+ `skill.json`（与 frontmatter 双写同步的机器可读元数据）+ `references/`（按需加载的参考文档）。三层渐进式披露：元数据常驻（发现用），入口正文触发时加载（路由用），`references/` 各章按需读取（执行用）。引用保持一层深——只允许 `SKILL.md` 直达 `references/` 各正文，references 内同目录互链，不产生第三层。

**两级平铺，唯一入口直达正文**：

1. `SKILL.md`（本文件）= **唯一入口**。前半部分是说明（什么时候用、怎么走），后半部分"章级路由索引"是最细路由表——被检方拿得出什么就查哪层，**每行链接直达对应章正文**。
2. `references/` = **判据正文 + 全貌图（平级成员）**：
   - 章正文 `01` 到 `10` + [附录 A](references/附录A-元规则.md)（检查者纪律）+ [附录 B](references/附录B-脚手架型通用接入模板.md)（脚手架型通用模板） = 判据落地（检查点、判据表、达标/不达标信号、输出物）。
   - [00-全局地图](references/00-全局地图.md) = 章树全貌（对象全景，穷尽该检查的全部面），**供全貌扫描与扩容，不是执行必经的第二层**。

没有第三层。层（P0-P3）与深度（L1/L2/L3）是标注在正文之上的"使用视角"，不另建文档、不做成菜单——正文一律按章落，路由表已按"被检方提供物"把层翻译成"该点开哪几章"。

## 什么时候用

- 要验收一个 Agent / 多 Agent 项目（自研或第三方交付），判它能不能立项、能不能上线、能给什么档。
- 要核对 Agent 项目是否真需要 Agent、系统部件是否真实接线、评测资产是否可信、安全底线是否达标、交付证据是否成立。
- 要产出一份第三方可复现的验收报告与档位结论。
- 不适用的场景（Out of Scope 细节见 [附录 A](references/附录A-元规则.md)）：模型基座与训练质量、纯 prompt 封装、通用公平性、组织级合规认证。

## 一次验收怎么走（选级别 -> 按级别跑 -> 出结论）

**第一步：选级别——四个级别包含制，选高自动包含低**：

| 检查级别 | 包含哪些章 | 产出 | 适合什么场景 |
|---|---|---|---|
| **轻度（初级检查）** | 第 1 章（定档问卷）-> 第 2 章（本体组成）-> 第 10 章（出结论） | 体检初判：骨架对不对、哪些面达标、初步结论。缺材料的层记"未评估"并自动封顶，不会拖垮结论 | "我就想快速看看这个项目骨架对不对" |
| **中度（中级检查）** | 轻度的全部 + 第 7 章（安全底线，红线必查不可跳） | 在轻度基础上加"能不能放出去"的安全判断，出结论 + 改进清单 | "我想知道能不能上线、安不安全" |
| **重度（高级检查）** | 中度的全部 + 第 3-6 章（评测/指标）+ 第 8-9 章（生产就绪/交付证据）-> 第 10 章完整收口 | 第三方可复核的完整验收报告：含评测资产核验、运行可观测、交付证据链 | "我要正式验收报告，合同/采购/上线用" |
| **自动（AI 全扫后自选）** | AI 先读项目源码与文档，判断该跑哪个级别，按判断结果执行；可交叉（如安全跑中度、评测跑重度） | 同选定级别，附带 AI 选级别理由说明 | "我也不确定该查多深，你先看看项目再决定" |

> **包含关系**：重度包含中度包含轻度。选了中度 = 自动跑轻度的全部 + 加 7 章；选了重度 = 自动跑中度的全部 + 加 3-6/8-9 章。不需要重复声明。自动级别不是第四种内容，而是让 AI 替你从前三个级别中选/交叉。
>
> **怎么选**：
> - **用户直接选**：用户对自己项目了解，直接说"跑轻度/中度/重度"即可，AI 不再自行判断。
> - **自动级别**：用户说"你帮我看看该跑哪个"，AI 先全扫项目源码与文档，给出级别建议与理由（"这个项目只有源码和文档，建议跑轻度；有安全需求可加中度"），可按面交叉（如本体跑轻度、安全跑中度），用户确认后开跑。

**1.1 四问不过 = 快速出口（任何检查级别都生效）**：第 1 章四问不过即直接判 Demo 终止，不跑后续章节。避免对薄壳项目（SDK wrapper / starter）跑完整流程。

**三条预期，先讲清再开跑**：
- 技能是"体检单"不是"考试"——你没有的材料 = 该层记"未评估"，不是失败；结论会因此封顶并写明缺什么，帮你看到补建优先级。
- 轻度就能出可用初判；要冲"生产候选"结论档必须跑重度（至少覆盖 L1 全项 + 相关 L2 深化）。
- 中度/重度下，每一章单独作为一次任务交给执行者即可，章收口会写明"这次产出 + 下一步接哪章"。

**第二步：按选定的检查级别执行**：

1. 读 [01-范围、形态与档位](references/01-范围、形态与档位.md)：被检方口答或文档即够。先证是否真需要 Agent（**四问不过 = 快速出口，判 Demo 终止，不跑后续**），再锁形态/技术栈/边界/成功标准/自主度档位，输出定档记录。
2. 按选定检查级别与形态决定后续哪些章上场：
   - **轻度**：1 -> 2 -> 10。缺材料的层记"未评估"，10 汇总出初步结论。
   - **中度**：1 -> 2 -> 7（至少红线 7.5 必查）-> 10。安全不达标的项进改进清单。
   - **重度**：1 -> 2 -> 3-5 -> 6-9 -> 10 顺章推。每章可开可合，不必一口气。
   - **自动**：AI 全扫项目后从前三个级别中选/交叉。选级别理由写入定档记录第 7 项。
3. 逐章逐判点执行：达标/警示/不达标标记，写证据，不达标与警示落整改建议。
4. 走完全部可达层后进 [10-验收结论与档位](references/10-验收结论与档位.md) 收口：否决项先挡门、加权定档、未证实项不混入、给改进闭环。
5. 全流程遵守 [附录 A](references/附录A-元规则.md) 的检查者纪律（先看 A.1 不评什么、A.2 反模式自查）。
6. 判不了或超出判据范围的项：直接联网检索官方文档/教程，给出来源链接并标注检索时间；**不编造出处、不猜测数字、不依赖本地任何文件**。

**执行硬协议（必执行、不可跳过——入口必经 / 收口必经 / 产出必落盘）**：
- **入口必经**：任何检查级别都从 [1 定档问卷](references/01-范围、形态与档位.md) 起步，先落一份**定档记录**；不允许跳过 1、直接点开某章充当入口。
- **收口必经**：任何要出验收结论的流程，跑完各自可达层后一律进 [10 收口](references/10-验收结论与档位.md) 产出**验收结论单**收尾；不得停在中途某章就算"验完"。
- **产出必落盘**：本技能全部产出——定档记录、各章判定表、验收结论单及附件——一律写成独立 Markdown 文件，默认存入**被检项目根目录**下新建 `acceptance/<被检对象名>/<YYYY-MM-DD>/`，按 `NN-<主题>.md` 命名（章号 + 章主题顺排，例如 `01-定档记录.md`、`10-验收结论单.md`）；目录与命名可在 1 定档时与被检方显式约定并写入定档记录第 8 项"输出目录声明"。
- **分批跑接续**：中度/重度检查级别每章收口**即时落盘**本层判定表；10 汇总只读取上述目录内的文件，不依赖会话记忆。跨会话、跨人员的复核与复验一律到该目录取件。

**开跑先复述（第 0 步，动判据前自证已读）**：选定检查级别后先输出三行再开工——（1）本次检查级别（轻/中/重/自动）与上场章；（2）输出目录（默认 `acceptance/<被检对象名>/<YYYY-MM-DD>/`，与定档记录第 8 项"输出目录声明"不一致时以声明为准并说明）；（3）预计产出文件清单（定档记录 + 本次上场各章判定表 + 10 收口三件）。复述不出的项 = 还没读到位，先回 [01](references/01-范围、形态与档位.md) / [10](references/10-验收结论与档位.md) 读完再动判据。

## 章级路由索引（以"被检方拿得出的材料"为轴）

下表是本技能最细的路由：被检方能提供到什么程度，就能查到哪一层；拿不出即该层记"未评估"，不硬套。P = 提供物分层（使用轴）；章号与判点号是稳定主键。

| 能提供到什么 | 层 | 覆盖章 | 每章一句话 |
|---|---|---|---|
| 口答或文档（形态/技术栈/目标边界/成功标准/自主度） | P0 定档问卷 | [1 范围、形态与档位](references/01-范围、形态与档位.md) | 必经入口 = 选层问卷：证需要 Agent、锁形态栈边界成功标准自主档，出定档记录 |
| 以上再加源码 + 文档 | P1 组成可查 | [2 系统组成与真实接线](references/02-系统组成与真实接线.md) | 按组成拆开逐块查齐不齐、接没接线，只判实现真实可用 |
| 以上再加测试集 / 自评基建 / 判分口径 / 目标值 | P2 评测资产 | [3 考卷与评测集](references/03-考卷与评测集.md)、[4 判分规则](references/04-判分规则.md)、[5 指标与目标值](references/05-指标与目标值.md) | 3 尺子可不可信；4 怎么判才对；5 多少算好（P2 收官） |
| 以上再加可运行部署 + 观测/审计/上线/交付证据 | P3 运行与交付证据 | [6 运行可观测](references/06-运行可观测.md)、[7 安全与权限底线](references/07-安全与权限底线.md)、[8 生产就绪与上线](references/08-生产就绪与上线.md)、[9 交付物与证据链](references/09-交付物与证据链.md) | 6 过程可见可回放；7 安全底线必查不可跳；8 能不能放出去；9 交得出什么、哪条证据能进结论 |
| 以上各层结论齐 | 收口 | [10 验收结论与档位](references/10-验收结论与档位.md) | 合成定论：否决先行、加权定档、未证实项清单、改进回归 |
| 全程 | 检查者纪律 | [附录 A 元规则](references/附录A-元规则.md) | 不评什么 + 反模式自查，读完才够格当检查者 |
| 脚手架型项目 | 通用接入模板 | [附录 B 脚手架型通用接入模板](references/附录B-脚手架型通用接入模板.md) | 红线清单模板 + 护栏/评估/反思接入位置示例 + 安全测试框架扩展模板，套进去改名字就能用 |

深度并存标注（另一视图，不另建文档）：L1 必查主干（约 P0 + P1 + 7 底线 + 6/8/9 基础件）；L2 深化（数字合格线 / judge 校准 / 反模式）；L3 专项触发（对抗深检 / 合规对照 / 9.6 业务六维）。P 轴管"这层需要给什么才能查"，L 轴管"这次查多深、哪些触发"。

## 打包与维护边界

- 打包单元 = 本技能文件夹整体（`agent-acceptance/` = `SKILL.md` 入口 + `skill.json` 元数据 + `references/` 判据正文）；本文件夹不依赖任何外部文件。
- 改判据只改 `references/` 对应章正文，不动章号与判点号（判点号是稳定主键，正文互引用它）；新增检查面先过 [00-全局地图](references/00-全局地图.md) 扩容三关再落正文。
- 技能作者的写作纪律、链接白名单、打包前自查清单等维护文档收在本目录 `docs/`（作者与维护者专用，执行验收无需读取；技能生态发布可不随包，但随项目仓库保留）。
