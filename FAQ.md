# FAQ

## 这是什么，解决什么问题？

agent-acceptance 是对 AI Agent / 多 Agent 项目做工程验收与定档的开放标准，以 Agent Skills 技能包形式发布。它回答三个问题：项目是否拿得出手、能否放得出去、应给予什么档位。它**不评模型强不强**，只查工程与证据。

## 与评测模型能力的榜单 / benchmark 有何不同？

本标准评估对象是工程与证据（组成接线、评测资产、判分规则、可观测性、安全底线、交付证据），不是模型能力。被检方不提供模型能力分数，而是提供源码、文档、测试集、运行观测等，逐层核对后给出档位结论与未证实项清单。

## 一次验收要跑全部十章吗？

不需要。第 1 章定档问卷是分流器，先问形态、技术栈、已有材料与自主度，据此决定后续开查哪些章。三种常见跑法：

- 快速初判：1 → 2 → 7 → 10
- 完整验收：1 → 2 → 3-5 → 6-9 → 10（分批多轮）
- 甲方收货：1 + 9 + 10（交付物与证据为主线）

缺材料的章记「未评估」，不拖垮整体结论，但会封顶档位并在报告中写明缺什么、补哪些可升档。

## 档位有哪些，凭什么定档？

五档：`block`（禁止放行）、`仅 Demo`、`原型`、`有限 Beta`、`生产候选`。档位由各层判定经第 10 章聚合规则合成：否决项先挡门、加权定档、未证实项不混入通过。档位高低与监管风险相互独立，涉及资金、隐私、人身安全的项目无论档位均须过安全底线。

## block 意味着项目彻底不行吗？

不是。`block` 表示「当前放出去会出问题，禁止交付」，通常触发自安全红线或证据不足以证明可信。整改后可复评、可升档，报告会给出未证实项清单与补齐优先级。

## 判据可第三方复核吗？

可以。全部判据自含于本仓库，不依赖包外内容。任何第三方可依 `references/` 正文独立检查并得到相同结论；校验脚本 `python scripts/verify.py` 做结构与语义双重复核。对外引用仅限官方一手来源并标注出处与版本。

## 判据与 GB/T 25000.51、OWASP Agentic AI Top 10 等框架什么关系？

它们是外部参照框架。本标准的判据以自身编号体系为主键，不与外部框架做一对一号映射；学习与核对覆盖时可对照使用（见第 1 章与全局地图）。

## 如何安装与使用？

将本仓库内容放入支持 Agent Skills 规范的技能目录，CodeBuddy 对应 `~/.codebuddy/skills/agent-acceptance/`，重启或重载技能索引后即可按名触发。安装也可一键完成：`python scripts/release.py all`（校验 + 打包 + 安装本机）。用法按角色见 README 的「不同用户，不同用法」段落。

## 如何贡献？

先读 CONTRIBUTING.md 与 docs/00 写作与引用规范。核心约束：判点号是稳定主键，改判据不动编号；新增检查面须过扩容三关（通用 / 公用 / 可判）；改动须仓库、发布物、本机三方同步；合入前 `python scripts/verify.py` 须 FAIL 0。
