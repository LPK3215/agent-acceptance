## 09-04 视觉资产统一与优化

- **设计 token**：确立项目视觉统一主题「深空蓝 #0369a1 + 琥珀金 #b45309 + 翠绿 #047857 + 紫罗兰 #6d28d9 + 警示红 #be123c + 中性灰 #94a3b8」。
- **docs/scripts/generate_overview.py**：修复 font-family 混入字号 bug；配色全面替换为设计 token；统一圆角与阴影；箭头改为单一 SVG marker。
- **docs/scripts/generate_badges.py**：修复同样字体 bug；value 区圆角补缺口；配色与 overview.svg 统一。
- **docs/assets/*.svg**：重跑脚本后两个 SVG 的视觉风格统一，无硬编码 Material 色残留。
- **project_overview/index.html**：
  - SVG1（数据流架构图）渐变与描边全部改用 CSS 变量（`--accent-2`、`--accent`、`--accent-3`），深浅主题自动适配。
  - SVG2（流水线架构图）补全章 10 收口节点（原死结构 `height="0"` 已修复为实体矩形节点），增加 hover 发光高亮与描边类。
  - 补 favicon：内联 SVG 盾牌 + 对勾，契合「验收」意象。
  - card-icon 硬编码色全部改为 `var(--accent-2)` / `var(--accent)` / `var(--violet)` 等主题变量联动。
- **docs/01-项目结构与流程图.md & 02-章间依赖与数据流.md**：Mermaid 流程图配色批量替换为主题 token，语义映射：蓝=章1/常规、琥珀=收口、绿=通过、紫=评测、红=拒绝、灰=跳过。
- **验证**：无头 Chromium 深浅主题分别截图，DOM 结构完整，无控制台报错，无硬编码色残留。

## 09-04 第二轮：展示数字与代码全量同步

- **核对真实值**：references/ 13 篇（含附录 B，2282 行）、判点 357（章 1=23/2=68/3=41/4=39/5=30/6=29/7=38/8=39/9=34/10=16）；docs/ 4 篇 590 行；scripts（verify.py 528 / release.py 168）；tests/ 4 文件（conftest 90 / test_verify 395 / test_release 160 / test_generate 207）；.github 22 行；dist zip 130.7 KB。
- **修正的展示漂移**：章 2 行数 289→290（script.js CHAPTERS + REF_DOCS）；verify.py 表格 454→528、release.py 141→168（index.html + script.js TREE）；dist 122→131 KB；质量保障段由"无单测无 CI"更正为 pytest+GitHub Actions+机械自检三层；目录树补 tests/ 与 .github/；README/CONTRIBUTING 补附录 B 与版本 1.1.0。
- **加固 verify.py**：check_project_overview 新增「展示页行数/文件数漂移」检查（references 总行数 + 各章行数登记），落实 README 声明的"展示页同步：章节行数"；main() 开头 `results.clear()` 修复测试间结果残留误报。
- **测试副作用修复**：test_verify.py::test_all_synced_passes 的 sample 与新检查不自洽 → 补 file/lines、篇数/行数、badge 计数，并 monkeypatch REF_DIR/KNOWN_REF_FILES。
- **结果**：verify.py FAIL 0 / WARN 3（仅官方外链提示，符合规范）；pytest 71 passed；重跑 release.py package 刷新 dist/agent-acceptance-1.1.0.zip。
- **坑**：python heredoc 内正则反斜杠会被吞（regex 匹配 0），但 verify.py 作为真实文件运行时正常；隔离调试正则务必写临时 .py 文件而非 heredoc。
## 09-04 第三轮：v1.2.0 发布（提交 / 推送 / 打标签）

- **版本号统一 bump 至 1.2.0**：skill.json / SKILL.md frontmatter / CONTRIBUTING / 全景展示页（index.html 品牌版本、副标题、发布流程、版本块、页脚；script.js tree badge 与 dist zip 名）/ 两张 SVG（重跑生成脚本从 skill.json 取数）全部对齐。index.html 第 1008 行「1.1.0 为判据优化版本」是历史事实陈述，刻意保留不 bump。
- **CHANGELOG 治理**：`v1.1.0` tag 已存在（指向 08ed798），但 main 超前 2 commit（CI + 附录B），且 c0406c0 把附录B/CI 错塞进 `[1.1.0]` 段（版本号落后的脏状态）。修复：还原 `[1.1.0]` 段为 08ed798 真实内容，新增 `[1.2.0]` 段记录本轮净改动（展示同步 + verify 加固 + 三层质量把关成形）。**不动已发布的 v1.1.0 tag**。
- **提交推送打标签**：`git add -A` → commit（chore(release): v1.2.0）→ `git tag -a v1.2.0` → push origin main + v1.2.0。dist/ 由 .gitignore 排除，不进库。
- **最终门禁**：verify.py FAIL 0 / WARN 3（仅官方外链提示，符合规范）；pytest 71 passed；release.py package 生成 agent-acceptance-1.2.0.zip（133.8 KB）。
