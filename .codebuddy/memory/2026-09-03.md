# 2026-09-03

## agent-acceptance 命名体系收紧（"主题词共享"原则）
- 背景：references 章文件名（NN-主题词）与章 H1、00-global-map 小节标题三处对应，全靠人肉同步。
- 改法（2026-09-03）：
  1. 10 个章 H1 全部去掉括注（原 01-03 是占位"（章节正文）"，04-10 带功能括注，风格断裂）→ 统一为 `# NN 主题词`；功能括注只留在地图小节标题 + 章内 `查什么` 行。
  2. 地图补上 07 小节缺失的功能括注（不越权、不上当、关键交人、数据可管）。
  3. docs/00 规范新增"命名纪律"小节：文件名 `NN-主题词.md`、章 H1 `# NN 主题词`（无括注）、地图 `## NN. 主题词（可带括注）`，共享同一主题词逐字一致；00 与附录 A 是特例。
  4. verify.py 新增 `check_title_topic`：机器校验 文件名主题词 == H1 主题词 == 地图小节主题词，改一处不同步即 FAIL。已跑通 FAIL 0 / WARN 3（官方外链）。
  5. 已重新 `python scripts/release.py all` 同步 zip 与本机安装副本。
- 观察：00-global-map.md 的英文文件名是有意的（skills.io 生态惯用 global-map），docs/00 称之为"地图"，正文互链不依赖文件名。

## agent-acceptance 第三轮纵深打磨（用户重申"格式内容段落标题一切可改"）
- 全量 read 了 SKILL.md / 01 / 00地图 / 10 / 附录A，逐字找格式与一致性缺陷，本轮改动：
  1. 模板补齐：01-03 章缺"本章边界（防越界三句）"头部块（04+ 均按 docs/00 骨架写的），已各补三句并确保引用的判点编号真实存在；docs/00 骨架注释补充"被检方提供物/使用方式"仅 01-03 入口章在用的可选附加行。
  2. 悬空口径修复："五能"作为未定义分类在 01/02/08 各出现一次（"对应五能之X一项的落点"），全库无定义，违反 R2 自含纪律——已改写为自含的"XX 能力面/治理面落点"表述，具体能力名保留。
  3. 00地图 A.2 行 `<50 条>`、`<反例集>` 尖括号残留（全文用 ** 加粗）→ 清理并与附录 A 表内口径统一。
  4. 00地图 5.2 行句内重复（"与步数/失败率/成本同报"两遍）→ 合并为"四件套同报"。
  5. 术语统一：00地图"扩容三铁律"→"扩容三关（通用/公用/可判）"（SKILL/README 及同文件下方均为"三关"）。
  6. SKILL.md 22/81 行结构描述漏 skill.json（写"= SKILL.md + references/"）→ 与 release/verify/docs/00/README 统一为含 skill.json。
- 经验：引用"小节判点编号"必须回查存在性；map/正文标题比对器只比编号不比标题文本，改括注安全。
- 状态：verify FAIL 0 / WARN 3（官方一手外链），release all 已重新同步 zip + 本机。

## agent-acceptance 元验收诊断（2026-09-03 第二轮全权优化）
- 用户完全放权后做了全包元验收：通读全部 12 个 references + SKILL/README/docs，逐项对照本包自己的验收方法论。
- 实测结论：正文判点互引零断链（357 判点全有真实索引）、地图↔正文小节双向全对齐、判点号连续无断号、01 章 6 列表是 G1-G4 矩阵合法结构。内容质量高，不需大改。
- 真正的空白在"机器护栏"：verify.py 之前只查链接/字符/章号/主题词三方，不守卫判点主键。本轮新增两检查：
  1. `collect_checkpoint_ids` + `check_ref_ids`：跨章判点号引用完整性——正文 x.y.z 引用必须存在于判点全集，防改判点号后引用悬空。
  2. `check_map_section`：地图↔正文小节编号双向对齐（00-global-map 承诺"章树全貌"，正文加/删小节不同步地图即 FAIL）。
  两条均挂在主流程 C 区、SKILL/README/00 豁免判定。README 一键检验行与 docs/00 打包边界说明已同步。
- 修正口径：docs/00 打包边界原来漏写 skill.json（写"SKILL.md + references/"），与 release.py/README 实际不符，已改"SKILL.md + skill.json + references/"。
- docs/00 判据表式样补注：档位对照矩阵（G1-G4 多列）是四列式样的合法例外，防未来维护误删。
- 教训：README 编辑时不能引入 ↔（U+2194），属 R4 黑名单箭头符号，verify 会 FAIL；改回用斜杠/普通连字符。
- 验证：FAIL 0 / WARN 3（官方外链），`python scripts/release.py all` 已重新同步 zip + 本机安装。

## agent-acceptance 技能包新增一键运维工具
- 参照 CodeBuddy 官方 skill-creator（quick_validate + package_skill 范式）给技能包配发布流水线，全部落地于仓库 `scripts/`：
  - `scripts/release.py`：唯一逻辑入口，子命令 check（=调用 docs/verify.py）/ package（校验后打 `dist/agent-acceptance-<版本>.zip`，zip 顶层带技能目录名 `agent-acceptance/`，解压即装）/ install（校验后同步到 `~/.codebuddy/skills/agent-acceptance/`）/ all（默认=check+package+install，一次跑完）。校验同进程只跑一次（_verified 缓存）。Python 3 标准库零依赖。
  - `scripts/verify.cmd`、`scripts/release.cmd`：Windows 双击入口。
  - `.gitignore`：忽略 `__pycache__/ *.pyc dist/ agent-acceptance-*.zip`。
- 发布物边界（README 已写"维护者速查"表）：`SKILL.md + skill.json + references/`；docs/ scripts/ .codebuddy/ 不进发布物。
- 实测 `python scripts/release.py all`：校验 FAIL 0 / WARN 3（均官方一手外链），zip 14 条目结构正确，已装入 `C:\Users\17538\.codebuddy\skills\agent-acceptance`。
- 仓库当前 git 无 remote、No commits yet；发布=打 zip + 装本机，未接远程推送。

## agent-acceptance 00-global-map 标题治理（2026-09-03）
- 用户重申"标题名称都可不合适就改"，先推地图标题病修复（章级主题词更名仍搁置待用户拍板风格，见下）。
- 本轮地图修改（verify FAIL 0 / WARN 3 回归通过）：
  1. 2 章 H3 英文附注对齐正文最新口径：2.4 Planning & Reasoning / 2.5 Tool & Skills / 2.7 Reflection / 2.8 Knowledge & RAG / 2.9 Document Parsing / 2.10 Data & Files / 2.11 State & Orchestration / 2.13 Multi-Agent（旧图残留 Feedback Loop/Knowledge Base/Workflow-Graph-checkpoint 等旧称）。
  2. 补 2.12 判点层：原只有 H3 无 H4（其它 2.x 均有），新增 #### 2.12.1-2.12.8 八个 H4，名与正文判据表一致（Run loop/Tool boundary/Checkpoint/Retry/HITL/Telemetry/Eval hook/Cost）。
  3. 章级 H2：1 章已有括注不改；2 章括注白话压缩为"（组成部件齐不齐、接线真不真）"（主题词仍=本体构成，与文件名一致）。
  4. 超长 H3 压缩（判据级细节留正文承载）：1.1 过度设计四问、1.2 载体形态、1.3 技术栈、1.6 G1-G4、3.1/3.7/3.8、5.2、9.6、A.2 六条。
  5. 9.6 括注修正为正文真实六维（最小闭环/复核卡位/工具写回/异常兜底/全程追踪/成果与风险边界）——原括注只有 5 项且"运营看板"只是 9.6.6 三件套之一，具误导。
  6. 补齐地图缺的正文同款括注：4.3 幻觉分型补 / Fact+mean^2、5.4 五层目标展开、5.5 首月合格线/3 月目标线、8.1 四可展开。
- 结构性经验：00-global-map 各小节行=可扫导航（引导正文），不承载判据；正文判据表/查什么行是全库唯一细节源，压缩标题前先确认正文已承载。
- 遗留未决：章主题词更名（用户点名"本体构成""项目定档与边界"听不懂）——已给两套候选（书面直白 / 大白话问答式）与范围选项（只 1/2 章 / 全 10 章），用户尚未选择即转推地图修复；地图修改均未触碰主题词，待命名决定后再三方同步（文件名+H1+地图主题词+SKILL 路由链接）。

## agent-acceptance 章主题词更名落地（2026-09-03 用户拍板，选书面直白路线）
- 1/2 章主题词更名并整库同步（git mv 保留跟踪，verify FAIL 0 / WARN 3 通过）：
  - 01 `项目定档与边界` -> `范围、形态与档位`（`01-范围、形态与档位.md`，H1 `# 1 范围、形态与档位`）
  - 02 `本体构成` -> `系统组成与真实接线`（`02-系统组成与真实接线.md`，H1 `# 2 系统组成与真实接线`）
- 连带同步共 10 文件（2 rename + 8 modify）：P1 层名 `本体可查` -> `组成可查`（SKILL 路由表 / 00 地图分层速览表 / 02 章头部角色行与收口行）；SKILL.md + skill.json description `本体部件` -> `系统部件`；README 流程"定档 -> 系统组成"与判据正文标签；docs/00 命名纪律两条示例文件名与 H1；docs/verify.py KNOWN_REF_FILES 数组；00 地图章主题行 27/35、P1 表行、直达链接文本；02 正文收口三处 `本体构成判定表` -> `系统组成判定表`。
- 默认保留（用户同意）：正文方法学语汇"本体层"、`行为本体在 2.4.5 判`、`2 本体缺件` 等约 40 处"本体"技术词，只清标题位与对外标签位；正文动作词"定档"（定档记录/定档问卷）与库内"P0 定档问卷"层名不动。
- 命名取向（项目约定）：章名/对外标签要**直白可懂、不用黑话当标题**；推荐时优先"书面直白"路线（与 3-10 章名词短语同构），非"大白话问答式"。
- 注意：本轮未跑 `release.py`，发布物 zip 与本机安装副本仍是旧章名；下次发布前 `python scripts/release.py all`。

## agent-acceptance 04 章更名补齐全库同构（2026-09-03）
- 用户追问"其他章节呢"，盘点确认 3-10 章原本即直白名词短语风格（考卷与评测集/指标与目标值/…），与 1/2 新名同构，无需再改；唯一异类是 04「判分器」（动词+器），用户拍板改为「判分规则」。
- 落地（git mv 保留跟踪 + 7 处同步，verify FAIL 0 / WARN 3 通过，grep 标题位零残留）：
  - rename `04-判分器.md` -> `04-判分规则.md`；H1 `# 4 判分器` -> `# 4 判分规则`；00 地图 H2 主题行与 P2 速览表行、直达链接文本/路径同步。
  - 跨文件章代称同步：SKILL.md 路由表链接（3/5 本就用全名链接文本，04 对齐全名）；05 头部"到 4 判分规则（怎么算对）"；03 收口"供 4 判分规则 / 5 指标"；verify.py KNOWN_REF_FILES。
  - 保留不动：正文方法学语汇"判分器/判分表/判分矩阵/判分器可信度"（02 的 ChatEval 引述、04 内部各句、06/08 分工句等，非章代称）。
- 状态：全 10 章命名体系统一为"直白名词短语"风格（1 范围形态档位 / 2 系统组成与真实接线 / 4 判分规则…）。发布物 zip 与本机安装副本仍含 1/2/4 三章旧名，待 `python scripts/release.py all` 刷新。

## agent-acceptance 提交推送 + gh 维护（2026-09-03）
- 用户要求"提交推送 + 用我的 gh 维护仓库（注意是私密仓库）"。现状核实：单 remote origin=https://github.com/LPK3215/agent-acceptance（PRIVATE）；gh 登录 LPK3215（scopes 含 repo/admin:org）。
- 提交 `671acd7 refactor: 章命名统一直白可读，01/02/04 主题词更名`（12 files，3 个 rename 99% 识别），已 push origin main（8c62dd3..671acd7）。
- gh 维护：仓库 description 原为空，已补"对 AI Agent / 多 Agent 项目做工程验收并给档位结论的 Agent Skills 技能包（判据自含，可第三方复核）"；visibility 复核仍 PRIVATE。未跑 release.py，发布物 zip/本机副本仍是旧章名（待下次发布刷新）。

## agent-acceptance 可发现性优化（2026-09-03，用户要求"假装公开"准备）
- 咨询结论固化：让"相近提示词"能搜到技能，主战场是 **description（+keywords/topics/README）**，不是仓库名；名称只守精确写法。原因：AI 触发靠读 description 语义匹配；GitHub 检索权重=仓库名精确 > README 全文 > topics > description。用户选择"假装公开"，后续会对仓库做其他动作。
- 已落地（verify FAIL 0 / WARN 3 通过）：
  1. skill.json + SKILL.md frontmatter `description`（verify 强制同源同文，≤1024 字）改写为"验收/评估/体检/定档 + 智能体/多智能体别称 + 何时用"语义网版，自然织词不堆砌。
  2. skill.json `keywords` 8→15：增 agentic / llm-agent / acceptance-testing / audit / quality-gate / ship-readiness / safety。
  3. GitHub topics（gh repo edit --add-topic，私有期即生效、公开后展示）：ai-agents / agent-skills / llm-agents / acceptance-testing / evaluation / agents。
- 维持不动：仓库名 `agent-acceptance`（全站无撞名、语义准）；README 正文已含充分检索词无需改。
- 网络备注：gh api 直连 topics 查询超时（20.205.243.168 抖动），但 gh repo edit 已 exit 0 成功。

## agent-acceptance 全库交叉引用细查 + README 使用者视角重写（2026-09-03）
- 用户要求"仔仔细细检查细节、交叉引用与规范；README 应从用户角度而非维护者角度介绍"。做法：verify.py 全量 + 人工逐章通读（00 地图 / 01-10 / 附录A / docs-00 规范）+ grep 旧名残留。
- 细查结论：FAIL 0 / WARN 3（官方一手外链，R3 应留）；verify.py 实际查 9 类（wikilink 清零 / R4 字符与破折号 / 链接死链与越包 / frontmatter-skill.json 同源 / SKILL 行数 / references 登记表 / 正文小节导航覆盖 / 文件名-H1-地图主题词三方一致 / 判点引用全集守卫 / 地图-正文双向对齐）；人工核对章间收口链（1→2→3/5→4→5→6→7→8→9→10 分诊无断）与跨章判点钩子语义（6.2.2 拖垮 6.3/6.4、8.5 消费 5.4、9.2.4→10.2）均正确；"判分器"残留 13 处全为正文方法词符合约定；旧名三词零残留。
- README.md 全文重写：原后 60% 为维护视角（目录结构表 / 维护者速查 / 维护须知），改为按 4 类使用者旅程组织（甲方验收方 / 交付团队开发者 / 外包给 AI 助手 / 学习者与第三方复核人），每类给"想要的 / 怎么做 / 会得到 / 从这里开始"，另附快速开场白、一次验收体验、可信度依据、五条边界声明；维护内容压成文末一段指路 docs。
- 教训：README 重写引入箭头 →（U+2192）2 处被 verify FAIL（R4 白名单无箭头），改顿号/逗号后 FAIL 0 / WARN 3。
- 状态：README 变更未提交、release.py 未跑；发布物 zip/本机副本仍落后（旧章名 + 旧 description），git 工作区仍挂着 description/keywords 与 README 两轮改动待收尾。
